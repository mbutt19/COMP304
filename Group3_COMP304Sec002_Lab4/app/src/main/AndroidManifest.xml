<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.group3_comp304sec002_lab4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group3_COMP304Sec002_Lab4">
        <activity android:name=".UpdateInfoActivity" android:parentActivityName=".StudentActivity"></activity>
        <activity android:name=".ViewClassroomInfoActivity" android:parentActivityName=".MainActivity"/>
        <activity android:name=".ClassroomActivity" android:parentActivityName=".ViewClassroomInfoActivity"/>
        <activity android:name=".StudentActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".LoginActivity" android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>